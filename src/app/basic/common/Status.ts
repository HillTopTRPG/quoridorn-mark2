import { StandImageInfo } from "@/app/basic/stand-image/StandImage";
import { ChatPaletteInfo } from "@/app/basic/chat-palette/ChatPalette";

export type StatusInfo = {
  name: string;
  standImage: StandImageInfo;
  chatPalette: ChatPaletteInfo;
};
