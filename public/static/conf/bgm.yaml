#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "PV停止"
  # タグ
  tag: "PV"
  # URL(空文字にすると停止命令扱い)
  url: ""
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: false
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "BGM停止"
  # タグ
  tag: "BGM"
  # URL(空文字にすると停止命令扱い)
  url: ""
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: false
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "SE停止"
  # タグ
  tag: "SE"
  # URL(空文字にすると停止命令扱い)
  url: ""
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: false
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "馬車道"
  # タグ
  tag: "BGM"
  # URL(空文字にすると停止命令扱い)
  url: "./static/bgm/o12.mp3"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: true
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "小さな行進曲"
  # タグ
  tag: "BGM"
  # URL(空文字にすると停止命令扱い)
  url: "./static/bgm/n26.mp3"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 50
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: true
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "華志の舞 (Kashi no Mai)"
  # タグ
  tag: "BGM"
  # URL(空文字にすると停止命令扱い)
  url: "https://www.youtube.com/watch?v=nq3YnT0km2o&t=15s"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: -3
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 100
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: true
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "ECHO【Gumi English】"
  # タグ
  tag: "BGM"
  # URL(空文字にすると停止命令扱い)
  url: "https://www.youtube.com/watch?v=cQKGUgOfD8U"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 8
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: true
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "DJ Sona's Ultimate Skin Music: Kinetic"
  # タグ
  tag: "BGM"
  # URL(空文字にすると停止命令扱い)
  url: "https://www.youtube.com/watch?v=uHJyAZtRrOY"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: true
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "[サビ]気まぐれメルシィ feat. 初音ミク"
  # タグ
  tag: "BGM"
  # URL(空文字にすると停止命令扱い)
  url: "https://www.youtube.com/watch?v=o1iz4L-5zkQ"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 57
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 83
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 30
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 20
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: true
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "Megalovania サンズ戦【四つ打ち 重低音 アレンジ】"
  # タグ
  tag: "BGM"
  # URL(空文字にすると停止命令扱い)
  url: "https://www.youtube.com/watch?v=CrkRWzsmu8E"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 1.2
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 20
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 20
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: true
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "[サビ]Bad Apple!!"
  # タグ
  tag: "BGM"
  # URL(空文字にすると停止命令扱い)
  url: "https://www.youtube.com/watch?v=9lNZ_Rnr7Jc"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 56
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 84
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 30
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 20
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: true
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "ダイス音"
  # タグ
  tag: "SE"
  # URL(空文字にすると停止命令扱い)
  url: "https://www.youtube.com/watch?v=MJMCFDNRJnc"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: regexp
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: "→ [0-9]+"
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: false
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: true
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: true
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "チーン1"
  # タグ
  tag: "SE"
  # URL(空文字にすると停止命令扱い)
  url: "./static/bgm/tin1.mp3"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: false
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: true
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "出題1"
  # タグ
  tag: "SE"
  # URL(空文字にすると停止命令扱い)
  url: "./static/bgm/question1.mp3"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: false
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: true
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "正解1"
  # タグ
  tag: "SE"
  # URL(空文字にすると停止命令扱い)
  url: "./static/bgm/correct1.mp3"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: false
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: true
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "不正解1"
  # タグ
  tag: "SE"
  # URL(空文字にすると停止命令扱い)
  url: "./static/bgm/incorrect1.mp3"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: none
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: ""
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: false
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: true
#=========#=========#=========#=========#=========#=========#
- # タイトル
  title: "廃墟と詩のＴＲＰＧ「詩片のアルセット」予告PV"
  # タグ
  tag: "PV"
  # URL(空文字にすると停止命令扱い)
  url: "https://www.youtube.com/watch?v=ZVexg7S2Vsk"
  # 音量(0~100)
  volume: 80
  # 再生開始位置(秒単位)
  start: 0
  # 再生終了位置(秒単位) マイナス指定で末尾からの時間
  end: 0
  # フェードインにかける時間(0.1秒単位）
  fadeIn: 0
  # フェードアウトにかける時間(0.1秒単位）
  fadeOut: 0
  # チャット連携モード(none: 連携なし, last: チャット末尾, regexp: 正規表現)
  chatLinkageType: last
  # チャット連携対象指定(チャット連携モードが0の場合は利用しない)
  chatLinkageTarget: "入室しました。"
  # リピート再生(true: リピートする, false: リピートしない)
  isRepeat: false
  # スタンバイ状態にして、再生タイミングで初期読み込み時間によるラグを軽減するかどうか
  isStandBy: false
  # 多重再生時は既存再生を優先する（BGM向け）
  isForceContinue: false
  # 強制新規再生（効果音向け）
  isForceNew: false
